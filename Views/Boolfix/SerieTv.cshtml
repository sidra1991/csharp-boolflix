@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = "_logLayout";
}
@model List<Series>


    <ul>
        @foreach (Series item in Model)
        {
            <li> @item.Name </li>
        <a class="btn btn-primary" asp-area="" asp-controller="CrudFilmSerieTv" asp-action="UpdateSeries" asp-route-id="@item.Id">modifica serie</a>

            <li>
                <a class="btn btn-primary d-block" style="color : white ; width : 100 px ; " href="@Url.Action( "ShowSeries" ,new {id = item.Id}) ">
                    dettagli
                </a>
            </li>
        }
    </ul>